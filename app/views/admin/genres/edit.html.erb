<div><h3>　ジャンル編集</h3></div>
<div>　</div>
<div>
  <table class="col-md-5">
    <tr>
      <td><h4>ジャンル名</h4></td>
      <%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>
      <td><%= f.text_field :name %></td>
      <td><%= f.submit '変更を保存',class:"btn btn-success" %></td>
      <% end %>
    </tr>
  </table>
</div>