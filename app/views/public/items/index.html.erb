<h1>左３使ってジャンル検索</h1>
<%= render 'list', genres: @genres %>

<p>１挟む</p>

<h1>商品一覧（全<%= @items.count %>件）</h1>
<% @items.each do |item| %>
<table>
  <tr><td><%= image_tag item.image.variant(resize_to_limit:[200,200]) %></td></tr>
  <tr><td>
    <%= link_to item_path(item.id) do %>
    <%= item.name %>
    <% end %>
  </td></tr>
  <tr><td>￥<%= item.price %></td></tr>
</table>
<% end %>